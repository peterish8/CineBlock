/**
 * Legacy stamp reader entry point.
 *
 * The original envelope/flap/letter animation remains in StampCard.tsx so it
 * can be restored or compared without being part of the new Glass page.
 */
export { default } from "../StampCard";
