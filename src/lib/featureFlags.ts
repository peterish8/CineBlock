/** Netflix / Prime / Disney+ / Hotstar / Apple TV+ streaming skins — nav + route hidden when false. */
export const ENABLE_STREAMING = false;

/** Netflix dark UI theme in the theme switcher — hidden when false. */
export const ENABLE_NETFLIX_THEME = false;
