interface:
  display_name: "CineBlock"
  short_description: "Repo-specific patterns and workflows for CineBlock"
  default_prompt: "Use the CineBlock repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true