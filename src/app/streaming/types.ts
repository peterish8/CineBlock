export type Platform = {
  id: string;
  name: string;
  tmdbId: string;
  logo: string;
  tagline: string;
  bg: string;
  accent: string;
  textColor: string;
  borderColor: string;
  glowColor: string;
};
